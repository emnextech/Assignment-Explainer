{
  "name": "assignment-explainer",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace shared",
    "build": "npm run build --workspace shared && npm run build --workspace backend && npm run build --workspace frontend",
    "dev": "npm run dev:frontend",
    "dev:frontend": "npm run build --workspace shared && npm run dev --workspace frontend",
    "dev:backend": "npm run build --workspace shared && npm run dev --workspace backend",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
